%h3= @room.name
=current_user.room
= @room.latitude

.occupants
  -if @room.occupied
    Currently in use by:

    -@room.users.each do |user|
      - if curren_user = user
        .user
          You
      - else
        .user
          =user.name
          =user.email

.actions
  -if current_user.room == @room
    =link_to "Check room in", room_checkout_path(@room), :class => 'checkout'#, :method => "PUT"
  -else
    =link_to "Check out room", room_checkout_path(@room), :class => "checkout"#, :method => "PUT


%p Your Coordinates:
%p#demo Click the button to get your coordinates:



