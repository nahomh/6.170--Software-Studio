%h3= @room.name

.actions
  -if @room.occupied
    -if current_user.room == @room
      =link_to "Check room in", room_checkout_path(@room)#, :method => "PUT"
    -else
  -else
    =link_to "Check out room", room_checkout_path(@room)#, :method => "PUT
=@room.longitude
=@room.latitude


%p#demo Click the button to get your coordinates:


