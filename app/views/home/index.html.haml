%h1 MITspaces
%br
-for room in @rooms

  .room
    =room.name
    =room.description
    -if room.occupied
      %span.busy
        Occupied
    -else
      %span.available
        Available

%br
=gmaps4rails(@json)
